import { FilterPositionPipe } from './filter-position.pipe';

describe('FilterPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
